flyway.locations=filesystem:migrations

# Connection details are intentionally NOT set here.
# Flyway reads FLYWAY_URL / FLYWAY_USER / FLYWAY_PASSWORD directly from the environment
# (see .env.sample) — nothing environment-specific or secret lives in this file.
